import { PhraseScanner } from './phrase-scanner';
import { PhraseCache } from './phrase-cache';

export { PhraseScanner, PhraseCache };

export const Hydra = {
  PhraseScanner,
  PhraseCache,
};

export default Hydra;
